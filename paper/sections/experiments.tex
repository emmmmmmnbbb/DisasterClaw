\section{Experiments}
\label{sec:experiments}

\subsection{Questions and Reporting Policy}
We organize the study around six research questions:
\begin{itemize}
    \item \textbf{RQ1:} Are damage probabilities calibrated enough for action selection?
    \item \textbf{RQ2:} Does the policy identify when reinspection is useful better than no, random, fixed, and heuristic controls?
    \item \textbf{RQ3:} Does reinspection improve damage judgment on evidence-bearing episodes?
    \item \textbf{RQ4:} What judgment or uncertainty gain is obtained per extra action and meter flown?
    \item \textbf{RQ5:} How robust are calibration and decisions on held-out events and degraded observations?
    \item \textbf{RQ6:} How does active reinspection affect navigation \(\SR\), \(\NE\), \(\SPL\), and path length?
\end{itemize}
We report all available evidence for these questions, including negative and compromised results.
Navigation gains are never inferred from perception-only metrics, and \(\semSR\) is never treated as strict success.

\subsection{RQ6: Navigation Success Remains Low}
\input{tables/navigation}

\tabref{tab:navigation} reports the historical 40-instruction, single-repeat E1 run under the YOLO proposer as a diagnostic baseline.
Strict success is low for every configuration, and the historical differences are not statistically significant.
The authoritative strict suite under the event-disjoint U-Net proposer is written to
\texttt{runs/benchmarks/paper\_strict\_unet\_v1/e1\_nav} (three paired seeds, hybrid grounding); replace the generated navigation table from that run before treating RQ6 numbers as final.
We do not interpret localization Dice or proposal recall as navigation success.

\subsection{RQ1: Calibration Helps In-Domain Confidence, Not Uniformly}
\input{tables/calibration}

Under the frozen event-disjoint split, temperature scaling changes test ECE from 0.095 to 0.037 while leaving accuracy unchanged at 0.765 (\tabref{tab:calibration}).
On the held-out events, the same validation-fitted temperature increases ECE from 0.046 to 0.084.
Brier score and NLL improve on test and worsen slightly on holdout.
Thus calibrated probabilities are useful for the in-domain test partition, but a single scalar temperature is not a sufficient robustness guarantee for held-out disasters.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{generated/calibration_reliability.pdf}
  \caption{Strict event-disjoint test-set reliability before and after
  scalar temperature scaling fitted on validation.}
  \label{fig:reliability}
\end{figure}

\subsection{RQ5: Strict Difference Attention Does Not Repeat the Old Holdout Gain}
\input{tables/difference_attention}

After event-disjoint retraining, concatenation and difference attention reach nearly identical accuracy on test and holdout (\tabref{tab:difference_attention}).
Calibrated test ECE slightly favors difference attention, while calibrated holdout ECE favors concatenation.
The earlier historical observation that difference attention substantially raised holdout accuracy therefore does not survive the strict protocol.
\input{tables/event_split}
\tabref{tab:event-split} records the frozen partition.
\input{tables/yolo_metrics}
Under the same split, the strict YOLO detector remains weak (\tabref{tab:yolo-metrics}): test mAP@$0.5=0.144$ with low recall.
\input{tables/loc_metrics}
Replacing only the building proposer with an event-disjoint ResNet34 U-Net raises localization quality (\tabref{tab:loc-metrics}): test Dice $0.750$, proposal recall@$0.5$ $0.524$, and mean building AP@$0.5$ $0.451$, with still stronger holdout Dice $0.783$.
Damage labels continue to come from the dual-temporal classifier; we therefore report localization as scaffolding relief for grounding, not as a new scientific contribution.

\subsection{RQ2--RQ4: Strict Reinspection Remains Underpowered}
\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{generated/review_sheets/review_01.jpg}
  \caption{Contact-sheet audit for the evidence-rich reinspection subset.
  Starts are outside the 25\,m success radius and inside the default
  observation radius; approval is geometry-checked and visually reviewed.}
  \label{fig:evidence-rich}
\end{figure}

\input{tables/reinspection}

We evaluate six policies on the frozen 40-task evidence-rich set with hybrid grounding and three paired seeds (\figref{fig:evidence-rich}, \tabref{tab:reinspection}).
Under the event-disjoint U-Net proposer, all six policies obtain strict
\(\SR=0\), \(\semSR=0.05\), mean \(\NE=587.6\) m, and zero SPL.
All 720 episodes finish without benchmark errors, but none contains a
damage-evidence observation or triggers reinspection; consequently judgment
accuracy, damage macro-F1, and measured \(\Delta U\) are zero.
Because every policy produces the same trajectory-level aggregate, no
pairwise superiority test is informative.
The stronger localizer therefore does not resolve the end-to-end grounding
bottleneck: language-model and VLM fallbacks were unavailable in this run,
and localized proposals did not yield target evidence that exercised the
controller.
These results do not support claims about when to reinspect, judgment gain,
or cost-normalized utility.

\subsection{Pre-Registered Strict Evaluation Status}
Event-disjoint manifests, perception checkpoints (dual-temporal classifier, ResNet34 localizer), calibration fits, and the six-policy reinspection protocol are frozen.
The U-Net-proposer E11 ledger is frozen at
\texttt{runs/benchmarks/paper\_strict\_unet\_v1/e11}; the matched E1 ledger
is still being completed under the same configuration.
YOLO-proposer ledgers remain available for rollback comparison only.
Remaining robustness suites (GPS noise and forced degraded observation) follow the same protocol once the U-Net E1/E11 ledgers are frozen.
No positive claim is made for H3--H4 because trigger coverage and unique
evidence-positive items are both zero in the U-Net E11 suite.
Appendix~\ref{app:protocol} records the hypotheses and denominators.
